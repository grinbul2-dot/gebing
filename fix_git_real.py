print("Because I squashed the `HEAD@{5}` into `7afc4da` earlier and then my subsequent changes were to `motion.js` ONLY... I completely lost the `game.js` fixes for guides and school name and the `style.css` fixes.")
print("Wait, my `git log` ONLY SHOWS `7afc4da`? Yes, I did `git commit --amend` and erased all previous history!")
print("Let's look at `git reflog` again to find the actual commit before I ruined it.")
